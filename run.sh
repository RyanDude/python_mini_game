#!/bin/bash

# pls execute chmod +x filename to allocate permission for execution, ty
# enter the virtual env, then execute the code
python3 -m venv venv
source venv/bin/activate
# pip install xxx
python game.py